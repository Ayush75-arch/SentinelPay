const steps = [["01", "₹", "Payment arrives", "A payment enters the SentinelPay decision pipeline.", ""], ["02", "◎", "Personal baseline", "Your normal spending behavior becomes the point of comparison.", "highlighted"], ["03", "⚡", "Behavioral scoring", "Machine learning measures how unusual the payment is for you.", ""], ["04", "✓", "Security verification", "Duplicate, velocity, travel and subscription checks run in real time.", ""], ["05", "94", "Clear next step", "Signals become a clear risk score and a recommended action.", "final-process"]];

function HowItWorks() {
  return <section className="how-section section-padding" id="how-it-works"><div className="landing-container"><div className="section-heading"><span className="section-tag">HOW SENTINELPAY DECIDES</span><h2>Personal signals in.<span> Clear action out.</span></h2><p>Every payment is assessed against your behavior and a focused set of real-time security controls before SentinelPay recommends what happens next.</p></div><div className="process-flow">{steps.flatMap(([number, icon, title, text, className], index) => [index > 0 && <div className="process-arrow" key={`${title}-arrow`}>→</div>, <div className={`process-card ${className}`.trim()} key={title}><div className="process-number">{number}</div><div className="process-icon">{icon}</div><h3>{title}</h3><p>{text}</p></div>])}</div><div className="intelligence-grid"><div className="intelligence-card"><div className="intelligence-icon">AI</div><div><h3>Behavioral intelligence</h3><p>Personal baseline, anomaly detection and contextual spending patterns.</p></div></div><div className="intelligence-card"><div className="intelligence-icon">SEC</div><div><h3>Security intelligence</h3><p>Velocity, duplicate, impossible-travel and biometric confirmation signals.</p></div></div></div></div></section>;
}

export default HowItWorks;
